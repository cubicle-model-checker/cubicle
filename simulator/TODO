# TODO

## Simulateur

- [.] Scale le text pour le fit dans les carrés
- [.] Centrer le text
- [.] Afficher constantes & Mat
- [.] Pouvoir changer la vitesse d'execution de la simulation
- [.] Optimisation : Accéder au model state uniquement a travers le simulateur. Set le model state uniquement a travers le simulateur. Permet de le calculer qu'une seule fois
			-> Nécéssiterais de déplacer les "get_vuv_for_proc" de Utils vers le simulateur.
- [.] print_scene : Afficher les transitions prises avec les arguments. Déplacer le dumper dans la print_scene ?
- [.] Compiler variable table correctement

## Model

- Le model devrait contenir les unsafe

## Simulateur

- Pour avoir un mode qui ne fait que lire une trace, il suffierait de set la trace puis de set un modèle vide:
Ainsi, aucune transition ne sera effectuée, mais on pourra voir quand même. Il faudrait que la trace prenne en compte un certain nb proc peut être 
- Simuelr les unsafe si spécifié par l'utilisateur (Ajouter option) 

## SceneLib

- Grid Compositor
- Get hovered proc
- On veut peut être afficher a l'écran les variables 

## Sample scene 
- On veut afficher si la simulation est en pause
- On veut afficher le current step

## Compilateur

- [.] On veut (peut-être) un paramètre -o pour spécifier le path
- Mettre le "max_float" dans Simulator.Utils plutôt que dans le cutils

## Traces

Tableau redimensionnable
{
	Array : 
	i : (Ou je suis dans l'array)
}

module T ( X : sig type t)
	type X.t t
		val length 	: X.t -> int			
		val start 	: X.t -> int -> unit (recalibrer position)
		val get : 	: X.t -> X.t				(Récup current elem)
		val next : X.t -> unity
		val position : X.t -> int (récup current position)

Implémenter la signature de la trace statique
Construire & Impélementer la signature de la trace dynamique
Utiliser les traces dans le model et dans le simulateur

Le simumateur devrait rendre une trace non dynamique lorsque demandée mais avoir une trace dynamique

# GraphicsLib

- Ajouter une librarie avec OCamlCanvas
- Ajouter librairie pour faire raiseau de pétri

## Bugs

- Il semblerait que si on remonte dans le simulateur avant la première transition il y ait un problème


