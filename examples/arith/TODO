Temporary TODO exclusively for arithmetic.

- Possible:
	Décomposition des Constante en facteurs premiers afin de permettre une
	simplification de ceux ci
	Ex :

	4 * A + 2 * B = 0
	<=> 2 * A + B = 0

- Trouver plus d'exemple de test, trouver des tests plus amusant (Sera utile
	pour le rapport)

- Faire rapport de stage 

- S'occuper de obvious / impossible

# Rien a voir avec arith 

- Serait intéréssant d'autoriser

array P[proc] : proc
array T[proc] : int

T[P[i]] -> Pas autorisé actuellement

- Serait intéréssant de pouvoir écrire

var A : bool

requires (A)

plutôt que 

requires (A = True)

- Permettre mise a jour non déterministe de array

T[i] = .; -> Pas autorisé actuellement
