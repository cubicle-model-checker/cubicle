
array PC[proc] : int
var Init : bool
weak var X : int

invariant (p) { PC[p] < 0 } 

init (p) { Init = False && PC[p] = 0 && X = 0 }

unsafe (p) { Init = True && PC[p] = 10 && read(p, X) = 9 }

transition t_start (p)
requires { Init = False }
{ PC[p] := 1; Init := True }

transition t_count (p)
requires { Init = True }
{ PC[p] := PC[p] + 1; write(p, X, PC[p]) }
